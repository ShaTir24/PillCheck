# expo-router pulls in optional web-only deps (@expo/ui, vaul, radix-ui) whose
# peer react-dom requirement conflicts with our React 19.2 pin. We don't build
# for web, so this is a false-positive peer conflict, not a runtime risk.
legacy-peer-deps=true
